import { UsdMmkPipe } from './usd-mmk.pipe';

describe('UsdMmkPipe', () => {
    it('create an instance', () => {
        const pipe = new UsdMmkPipe();
        expect(pipe).toBeTruthy();
    });
});
